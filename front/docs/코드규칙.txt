1.변수명
    -최대한 직관적으로 명시,
    -줄임말은 최대한 피하고 만약에 줄이면 줄임말 리스트 작성,
        ex) 
        button => btn
        current => cur
    -리스트는 복수형, 리스트아이템은 단수형으로 지정,
        ex) awards.map(award,index=>{...})   //  O
        ex) awards.map(list,index=>{...})   //  X

2.State규칙
	-toggle state는 is(Name),setIs(Name)
	ex)const [isEdit,setIsEdit]=useState(false);
	
3.evenet시 발동 함수는 인라인 작성 x
	ex)<DelBtn onClick={()=>{ const a=fsldkaflka}) // X
	    <DelBtn onClick={onClickDel} // O

4.any 쓰지말기

2.컴포넌트 단위
wrapper
⬇️
contatiner
⬇️
box
⬇️

